{
  "name": "grok-desk-app",
  "version": "0.2.0",
  "private": true,
  "description": "Standalone Grok Desk desktop shell (Electron) over grok agent stdio",
  "main": "out/main.js",
  "scripts": {
    "postinstall": "node scripts/ensure-electron.js",
    "ensure-electron": "node scripts/ensure-electron.js",
    "compile": "tsc -p .",
    "start": "npm run ensure-electron && npm run compile && node scripts/run.js",
    "dev": "npm run ensure-electron && npm run compile && node scripts/run.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "electron": "^35.7.5",
    "typescript": "^5.4.0"
  }
}
